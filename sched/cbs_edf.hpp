#ifndef CBS_EDF_H
#define CBS_EDF_H

#include "../common/common.hpp"
#include "../task/job.hpp"
#include "../task/task_cbs.hpp"
#include "../task/runqueue.hpp"

#include "../sim_engine/sim_engine.hpp"

namespace sched {
  // void move_to_inactive_cbs_edf(task::Task_CBS * tau);
  // void task_arrival_cbs_edf(task::Job *job, int sig);
  // void task_complete_cbs_edf(task::Job *job, int sig);
  // void act_cont_timer_fired_cbs_edf(task::Job *job, int sig);
  // void main_timer_fired_cbs_edf(task::Job *job, int sig);
  // bool resched_cbs_edf(task::Runqueue *rq);
  // bool allocate_task_proc_cbs_edf(task::Task * tau, int alg);
}
#endif
